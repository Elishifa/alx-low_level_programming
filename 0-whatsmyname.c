#include <stdio.h>
#include "main.h"

/**
 * main - prints program name, followed by a new line.
 * @argc: argument count
 * @argv: containing command line argument
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
